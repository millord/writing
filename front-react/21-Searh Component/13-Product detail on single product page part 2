
1-create a function called showStock(product.quantity)

-add a <br/> below

just above showViewButton
and pass as argument the product quantity

2-create the function just below the showAddToCartButton

const showStock = (quantity) => {
  return quantity > 0 ? (
    <span className="badge badge-primary badge-pill">In Stock</span>
  ) : (
    <span className="badge badge-primary badge-pill">Out of Stock</span>
  );
};


3-Go to the Card component and in the card-header add
name, I mean, card-header name

4-Copy to the styles.css the styles from the source code.




