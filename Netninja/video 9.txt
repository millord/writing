Video 9 intro to hooks



By using a hook you can have state in a functional component

what is useState use for?
useState === use state within a functional component

what is useEffect use for?
useEffect === run code when a compoent renders (or renders)

what is useContext use for?
useContext === consume context in a functional component


----------------------------

create a new app called hooksapp



1-do some clean up and leave App.js, index.js and index.css

2-create a component folder and inside the file create the SongList.jsj component.
3-Make the SongList.js component to be a functional one.
4-insise render a list of songs and import the component into the App.js component.

